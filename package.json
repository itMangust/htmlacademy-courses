{
  "scripts": {
    "test": "editorconfig-cli && remark docs/**/*.md"
  },
  "precommit": "npm test",
  "editorconfig-cli": [
    "docs/**/*.md"
  ],
  "remarkConfig": {
    "plugins": [
      "remark-preset-lint-recommended"
    ]
  },
  "devDependencies": {
    "@htmlacademy/editorconfig-cli": "^1.0.0",
    "precommit": "^1.2.2",
    "remark-cli": "^8.0.1",
    "remark-preset-lint-recommended": "^4.0.1"
  }
}
